{
  "name": "landing",
  "description": "The Hermes landing page",
  "version": "0.0.0",
  "main": "src/index.html",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite src/",
    "build": "rm -rf dist/ && vite build -c vite.config.js src/ && mv src/dist/ dist/ && cp -r src/assets/* dist/assets/"
  },
  "dependencies": {
    "animate.css": "^4.1.1",
    "halfmoon": "^1.1.1",
    "lit": "^2.4.0",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "@lit/localize-tools": "^0.5.0",
    "vite": "^2.7.2"
  }
}
